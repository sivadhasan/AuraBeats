import React, { useContext } from 'react'

const Home = () => {
  // let {authUser} = useContext(AuthUserContext);
  // console.log(authUser);
  return (
    <div>Home</div>
  )
}

export default Home